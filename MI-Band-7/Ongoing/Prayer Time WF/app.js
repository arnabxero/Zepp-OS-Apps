try {
  (() => {

    var _app = __$$hmAppManager$$__.currentApp;
    'use strict';

    _app.app = DeviceRuntimeCore.App({
      globalData: {},
      onCreate(options) {
      },
      onShow(options) {
      },
      onHide(options) {
      },
      onDestory(options) {
      },
      onError(error) {
      },
      onPageNotFound(obj) {
      },
      onUnhandledRejection(obj) {
      }
    });
    /*
    * end js
    */

  })()
} catch (e) {
  console.log(e)
}